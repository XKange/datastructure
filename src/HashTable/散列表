为了充分利用空间，散列表的长度往往小于关键字值域大小。所以必然会产生碰撞，解决碰撞的方法有：
1.链接法：把散列在同一槽的所有元素都放在一张链表里。
2.开放寻址法：
    线性探查
    二次探查
    双重散列

散列函数

1.除数散列法（常用）：通过取k除以m的余数，来将关键字k映射到m个槽的某一个中去
2.乘法散列法：第一步用关键字k乘以常数A（0<A<1），并抽取出kA的小数部分。然后用m乘以这个值，再取结果的底。
    A 可以取黄金分割0.618
3.全域散列